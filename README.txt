Hello fellow coder!

This code was compiled using the default standard on the os1 server.

To compile use the following command:
gcc ./smallsh.c -std=c99 -D_POSIX_C_SOURCE=200809L -o smallsh

To execute use the following command:
./smallsh